[pytest]
# Only collect the project test suite.  This prevents generated evidence under
# `artifacts/` from ever being treated as a test directory.
testpaths = tests
# Do not create `.pytest_cache`; restricted Windows folders can otherwise turn
# a successful test run into a cache warning.
addopts = -p no:cacheprovider
